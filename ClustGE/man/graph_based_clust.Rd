% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{graph_based_clust}
\alias{graph_based_clust}
\title{Perform graph-based clustering (Louvain community detection) on expression data}
\usage{
graph_based_clust(
  se,
  k = 5,
  assay_name = "counts",
  target = c("samples", "genes"),
  annotate = FALSE
)
}
\arguments{
\item{se}{SummarizedExperiment or ExpressionSet object.}

\item{k}{Number of nearest neighbors for graph (default 5).}

\item{assay_name}{Name of assay (SummarizedExperiment only).}

\item{target}{"samples" or "genes".}

\item{annotate}{Logical; if TRUE, add cluster IDs to object metadata.}
}
\value{
igraph communities object, or list(object, clustering) if annotate = TRUE.
}
\description{
Perform graph-based clustering (Louvain community detection) on expression data
}
\examples{
library(SummarizedExperiment)
mat <- matrix(rnorm(50), nrow = 10)
se <- SummarizedExperiment(assays = list(counts = mat))
comm <- graph_based_clust(se, target = "samples", k = 3)
res <- graph_based_clust(se, target = "samples", k = 3, annotate = TRUE)
names(res)
}
