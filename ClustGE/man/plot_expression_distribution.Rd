% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_expression_distribution}
\alias{plot_expression_distribution}
\title{Plot Distribution of Gene Expression Values}
\usage{
plot_expression_distribution(se, bins = 50)
}
\arguments{
\item{se}{A SummarizedExperiment or ExpressionSet object.}

\item{bins}{Number of bins for the histogram (default = 50).}
}
\value{
A ggplot object.
}
\description{
Creates a histogram of expression values from the counts assay.
}
\examples{
library(SummarizedExperiment)

set.seed(1)
mat <- matrix(rpois(50, lambda = 10), nrow = 10)
rownames(mat) <- paste0("gene", 1:10)
colnames(mat) <- paste0("sample", 1:5)

se <- SummarizedExperiment(assays = list(counts = mat))

plot_expression_distribution(se, bins = 20)
}
