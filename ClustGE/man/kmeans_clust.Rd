% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{kmeans_clust}
\alias{kmeans_clust}
\title{Perform k-means clustering on expression data}
\usage{
kmeans_clust(
  se,
  centers,
  assay_name = "counts",
  target = c("samples", "genes"),
  nstart = 10,
  annotate = FALSE
)
}
\arguments{
\item{se}{SummarizedExperiment or ExpressionSet object.}

\item{centers}{Number of clusters.}

\item{assay_name}{Name of assay (SummarizedExperiment only).}

\item{target}{"samples" or "genes" — whether to cluster samples or genes.}

\item{nstart}{Number of random starts (default 10).}

\item{annotate}{Logical; if TRUE, add cluster IDs to object metadata.}
}
\value{
kmeans object, or list(object, clustering) if annotate = TRUE.
}
\description{
Perform k-means clustering on expression data
}
\examples{
library(SummarizedExperiment)
mat <- matrix(rnorm(50), nrow = 10)
se <- SummarizedExperiment(assays = list(counts = mat))
km <- kmeans_clust(se, centers = 2, target = "samples")
res <- kmeans_clust(se, centers = 2, target = "samples", annotate = TRUE)
names(res)
}
