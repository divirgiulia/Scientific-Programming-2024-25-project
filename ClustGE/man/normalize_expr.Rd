% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization.R
\name{normalize_expr}
\alias{normalize_expr}
\title{Normalize Expression Data}
\usage{
normalize_expr(se, method = c("log", "quantile"), standardize = TRUE)
}
\arguments{
\item{se}{A SummarizedExperiment or ExpressionSet object.}

\item{method}{Normalization method: "log" (log2(x+1)) or "quantile".}

\item{standardize}{Logical; if TRUE, scale genes to mean 0, sd 1.}
}
\value{
A normalized object of the same type as input.
}
\description{
Applies normalization to gene expression data.
}
\examples{
library(SummarizedExperiment)
mat <- matrix(rpois(40, lambda = 10), nrow = 10)
rownames(mat) <- paste0("gene", 1:10)
colnames(mat) <- paste0("sample", 1:4)
se <- SummarizedExperiment(assays = list(counts = mat))

se_norm_log <- normalize_expr(se, method = "log", standardize = TRUE)

se_norm_q <- normalize_expr(se, method = "quantile", standardize = FALSE)
}
