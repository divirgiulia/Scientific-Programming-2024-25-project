% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment_analysis.R
\name{perform_enrichKEGG}
\alias{perform_enrichKEGG}
\title{Perform KEGG enrichment analysis on gene clusters}
\usage{
perform_enrichKEGG(
  se,
  target = "genes",
  organism,
  keyType = "kegg",
  pAdjustMethod = "BH",
  pvalueCutoff = 0.05,
  qvalueCutoff = 0.2
)
}
\arguments{
\item{se}{SummarizedExperiment or ExpressionSet object with cluster annotations.}

\item{target}{"genes" — KEGG enrichment is performed on gene clusters.}

\item{organism}{KEGG organism code (e.g., "hsa" for human, "mmu" for mouse).}

\item{keyType}{Type of gene identifiers ("kegg", "ncbi-geneid", or "ENTREZID").}

\item{pAdjustMethod}{Method for multiple testing correction (default "BH").}

\item{pvalueCutoff}{P-value cutoff (default 0.05).}

\item{qvalueCutoff}{Q-value cutoff (default 0.2).}
}
\value{
List of enrichKEGG results per cluster.
}
\description{
Perform KEGG enrichment analysis on gene clusters
}
\examples{
library(SummarizedExperiment)
mat <- matrix(rpois(40, 10), nrow = 10)
rownames(mat) <- paste0("gene", 1:10)
se <- SummarizedExperiment(assays = list(counts = mat))
SummarizedExperiment::rowData(se)$Cluster <- rep(1:2, each = 5)
results <- perform_enrichKEGG(se, organism = "hsa", keyType = "ncbi-geneid")
}
