% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering_plots.R
\name{plot_pca}
\alias{plot_pca}
\title{PCA Plot of Samples #' #' Performs Principal Component Analysis on the counts assay and plots the first two principal components.}
\usage{
plot_pca(se, color_by = NULL)
}
\arguments{
\item{se}{A SummarizedExperiment or ExpressionSet object.}

\item{color_by}{Column name in sample metadata (colData/pData) to color points (optional).}
}
\value{
A ggplot object.
}
\description{
PCA Plot of Samples #' #' Performs Principal Component Analysis on the counts assay and plots the first two principal components.
}
\examples{
library(SummarizedExperiment)
library(S4Vectors)
mat <- matrix(rnorm(100), 10, 10)
colData <- S4Vectors::DataFrame(Group = rep(c("A", "B"), each = 5))
se <- SummarizedExperiment(assays = list(counts = mat), colData = colData)
plot_pca(se, color_by = "Group")
}
