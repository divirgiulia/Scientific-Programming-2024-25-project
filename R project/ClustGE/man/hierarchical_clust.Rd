% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{hierarchical_clust}
\alias{hierarchical_clust}
\title{Perform hierarchical clustering on expression data}
\usage{
hierarchical_clust(
  se,
  method = "euclidean",
  clust_method = "complete",
  assay_name = "counts",
  target = c("samples", "genes"),
  annotate = FALSE,
  k = NULL
)
}
\arguments{
\item{se}{SummarizedExperiment or ExpressionSet object.}

\item{method}{Distance method (default "euclidean").}

\item{clust_method}{Clustering method (default "complete").}

\item{assay_name}{Name of assay (SummarizedExperiment only).}

\item{target}{"samples" or "genes" — whether to cluster samples or genes.}

\item{annotate}{Logical; if TRUE, add cluster IDs to object metadata.}

\item{k}{Number of clusters (only used if annotate = TRUE and method is hclust).}
}
\value{
hclust object, or list(object, clustering) if annotate = TRUE.
}
\description{
Perform hierarchical clustering on expression data
}
\examples{
library(SummarizedExperiment)
mat <- matrix(rnorm(50), nrow = 10) # 10 genes x 5 samples
se <- SummarizedExperiment(assays = list(counts = mat))
hc <- hierarchical_clust(se, target = "samples")
res <- hierarchical_clust(se, target = "samples", annotate = TRUE, k = 2)
names(res)
}
